DB_USER=crafthub
DB_PASSWORD=crafthub_pass
DB_HOST=postgres
DB_PORT=5432
DB_NAME=crafthub_db

JWT_SECRET=change_me_dev
PORT=8080
ADMIN_BOOTSTRAP_SECRET=dev_admin_bootstrap_123
TOPUP_AUTO_CONFIRM=true

# Application base URL (used to build password reset links)
APP_URL=http://localhost:3000
# Optional: override reset token expiry (minutes)
PASSWORD_RESET_EXP_MINUTES=30

# Redis (for Asynq job processing)
REDIS_HOST=redis
REDIS_PORT=6379
# Alternatively set REDIS_ADDR=localhost:6379 when running locally

# SMTP (for real emails)
# Configure to enable actual mailing in alerts handlers
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USERNAME=apikey_or_username
SMTP_PASSWORD=your_password_or_api_key
SMTP_FROM=CraftHub <no-reply@yourdomain.com>

# Mail provider selection
# Set to 'plunk' to use Plunk HTTP API instead of SMTP
MAIL_PROVIDER=smtp

# Plunk configuration (used when MAIL_PROVIDER=plunk)
PLUNK_API_KEY=sk_xxx
PLUNK_FROM=CraftHub <no-reply@yourdomain.com>
PLUNK_API_URL=https://api.useplunk.com/v1/send
